# 動作確認メモ

## 2026-08-20: 記事一覧ページ

- URL: `http://localhost:8081/sancstudy/articles.html`
- Supabaseの `articles` テーブルに公開済みレコードがまだない状態で、ページはエラーなく「公開中の記事はまだありません。」という空状態を表示した。
- ヘッダーの「記事」導線、フッターの「記事」導線、Cookieバナー、共通ナビゲーションが表示された。
- 画面上の空状態は2回目の確認時に安定しており、JavaScriptの構文・初期化エラーによる停止は確認されなかった。

次回確認: テストデータ登録後に一覧・詳細・著者リンク・カテゴリを検証する。

## 2026-08-20: 運営・更新情報ページ

ローカル環境で `about.html` と `news.html` を確認した。Supabaseに公開データが未登録の状態でも両ページはエラーなく動作し、運営ページには「公開中の運営メンバー情報は準備中です。」「公開中の沿革は準備中です。」、更新情報ページには「公開中の更新情報はまだありません。」という空状態が表示された。旧来の固定「ツール担当」カードは運営ページから除去されている。

記事導線はナビゲーションとフッターに表示され、更新情報のカテゴリタブも表示された。次回は実データ登録後に、バッジ・制作者リンク・記事本文・カテゴリ絞り込み・RLS公開範囲を確認する。

## 2026-08-20: トップページの制作者表示

公開済みの画像アセットをローカル検証環境へ復元した後、トップページを再確認した。ヒーロースライダーはページ読込直後から漢字大戦を表示し、ロゴ、カード画像、矢印、ドットが描画された。Supabaseの公開データから、漢字大戦・LEAP OVER QUESTの制作者として `hourai-carrot`、Prime Striker・宿題マネージャーの制作者として `beeeeach` が表示され、各制作者チップはプロフィールURLを持つことを確認した。

現時点でメンバー画像URLは未登録のため、チップには安全な文字イニシャルのフォールバックが表示される。初期データSQLが実行された状態では、公開コンテンツ数は4、公開メンバー数は2として反映された。

## 2026-08-20: プロフィール・更新情報の実データ表示

`profile.html?member=hourai-carrot` で、`hourai-carrot` の表示名、ゲーム開発の役職、青色の開発者バッジ、紹介文、漢字大戦とLEAP OVER QUESTの担当コンテンツが表示された。担当コンテンツの外部リンクも生成されている。

`news.html` で、Supabaseの公開済み初期更新情報が日付・サイトカテゴリ・見出し・本文・著者制作者チップとともに表示された。著者制作者チップは `profile.html?member=hourai-carrot` への内部リンクを持つ。

## 2026-08-20: JavaScript・記事サニタイズ

トップページのブラウザコンソールには新規のJavaScriptエラーが表示されなかった。記事本文サニタイズには、`script`要素、`onclick`属性、`javascript:`リンク、`javascript:`画像URLを含む入力を与えて確認した。結果として、スクリプト実行フラグは設定されず、`script`要素数は0、イベント属性は除去、危険なリンクの`href`は除去、危険な画像要素は除去された。許可した`article-lead`クラスは保持された。

## 2026-08-20: テスト記事メタデータ登録

Supabase SQL Editorでテスト記事 `sample-article` の登録を実施した。初回はエディタへの自動入力時に末尾の余分な括弧が残り、PostgreSQLの構文エラー `42601` となった。この失敗ではデータの変更は発生していない。

SQL本文をMonaco EditorのモデルAPIで正しい内容に置換して再実行したところ、`Success. No rows returned` が表示された。これにより、公開済みの記事メタデータが `articles` テーブルに登録された。

## 2026-08-20: 記事一覧・詳細の実データ検証

`articles.html` で、Supabaseに登録した `sample-article` がタイトル、要約、カテゴリ、公開日、著者名とともに表示された。記事カードは `article.html?slug=sample-article` へ正しく遷移する。

`article.html?slug=sample-article` で、GitHub Pages構成の `articles/sample-article.html` を本文として読み込み、見出し、段落、コード、注意枠、箇条書きを表示できた。ページタイトルは記事タイトルへ更新され、著者チップは `hourai-carrot` のプロフィールへ遷移する。記事本文のサニタイズは別途悪性入力で確認済みであり、実記事の許可済み要素も正しく表示された。

## 2026-08-20: トップページ回帰確認

初回表示でヒーローの1枚目「漢字大戦 KANJI WAR」が白紙にならず表示された。次スライド操作後も別コンテンツのスライドへ切り替わり、背景画像・見出し・CTAが維持された。ページの動的制作者チップは `hourai-carrot` と `beeeeach` を正しく表示し、レビュー欄は承認済み3件、平均4.0、星表示とともに描画された。

## 2026-08-20: ナビゲーション回帰確認

PC表示でもハンバーガーメニューが開き、ホーム、ゲーム、ツール、使い方、運営について、更新情報、記事の全導線が表示された。`Escape` キーでメニューが閉じ、背面のヒーロースライダーも通常表示へ復帰した。

## 2026-08-20: モバイル表示確認（375px幅）

ヘッドレスブラウザで375×812pxの表示を確認した。トップページではロゴ、ハンバーガーボタン、ヒーロー画像、スライド文言、CTAが横方向のはみ出しなく表示された。記事一覧では見出し、説明文、記事カード領域が1カラムで適切に収まり、横スクロールは確認されなかった。Cookieバナーは初回表示時に下部コンテンツの一部へ重なるが、同意または設定案内を選べる状態であり、操作後は通常の閲覧領域を確保できる。

スマホ幅の記事詳細では、戻る導線、記事見出し、要約、公開日、著者チップ、本文が1カラムに収まり、横方向の表示崩れは確認されなかった。

## 2026-08-20: RLS・匿名アクセス検証

公開用キーのみを用いた匿名REST APIで、公開済みの記事 `sample-article` は取得でき、`is_published=false` 条件の取得結果は空配列だった。匿名の `articles` INSERT はHTTP 401（RLS違反）、既存記事へのPATCHはHTTP 200かつ空配列で更新対象0件となり、記事タイトルが変更されていないことを再読込で確認した。匿名の `members` INSERT と `updates` INSERT も、いずれもHTTP 401（RLS違反）で拒否された。

適用済みSQLの方針どおり、`articles`、`updates`、`content_items`、`site_history` は `is_published = true` のみ、`members` は `is_active = true` のみを匿名SELECTの条件としている。匿名向けのINSERT・UPDATE・DELETEポリシーは定義されていない。

## 2026-08-20: JavaScriptエラー最終確認

トップページ読み込み後のブラウザコンソールには出力・エラーがなかった。ヒーロー、共通ナビゲーション、Supabaseの公開データ表示、レビュー表示を含む起動処理がエラーなく完了した。

## 2026-08-20: ヒーロースライダー自動送り確認

トップページを初期状態から読み込み、5.5秒待機後にアクティブなスライドとドットの一致を確認した。ロード処理時間を含む待機時点では3枚目がアクティブで、初期の1枚目から自動送りが継続していることを確認した。初期表示・手動操作・自動送りのいずれでも白紙のヒーロー状態は再現しなかった。
