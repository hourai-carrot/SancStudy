# SancStudy アイコン読込修正版・納品メモ

## 修正内容

制作者アイコンが一部のスマホで文字フォールバックのままになる問題を修正しました。

従来の実装は、`loading="lazy"` を付けた画像をDOMへ追加せず、`load` が成功した後だけ追加していました。この順序では、モバイルブラウザが画像を読み込み対象と見なさず、`load` が発火しないことがあります。

修正版では、画像をアバターコンテナへ先に追加してからURLを設定します。画像が読み込まれるまでは名前の先頭文字を表示し、成功時のみ画像へ置き換えます。画像取得に失敗した場合は画像要素だけを除去し、文字フォールバックを残すため、壊れた画像アイコンは表示されません。

全アバターに共通の `member-avatar` クラスを付与したため、制作者チップ、運営チーム、個人プロフィールの画像サイズ・円形トリミング・`object-fit: cover` が一貫します。

## 変更ファイル

| 相対パス | 内容 |
|---|---|
| `js/main.js` | アイコン読込の循環を解消し、成功時の画像表示・失敗時の文字フォールバックを実装。前回追加したホーム最新記事とメニュー外タップ機能も含みます。 |
| `css/style.css` | プロフィールアイコンの半分サイズ設定と共通アバター表示に対応します。 |
| `index.html` | 月間利用者数の削除と、ホームの「最新の記事」セクションを含みます。 |
| `about.html` | 「SancStudyの歩み」を削除済みです。 |

## 検証結果

| 確認項目 | 結果 |
|---|---|
| `hourai-carrot` の制作者チップ | 画像要素が存在し、`complete=true`、画像幅764pxとして読み込み完了を確認しました。 |
| 個人プロフィール | 画像が読み込み完了後に表示され、PCで56px × 56pxの小型円形アバターを確認しました。 |
| 画像読込失敗時 | 存在しない画像URLで検証し、画像要素を除去して文字 `F` のフォールバックを維持しました。 |
| `beeeeach` の表示 | 現在はSupabaseの公開プロフィールに画像URLが未登録のため、仕様どおり文字 `B` を表示します。画像URLを登録すれば同じ処理で画像表示へ切り替わります。 |
| JavaScript | `node --check` とブラウザ実行時にエラーがないことを確認しました。 |
| 既存UI調整 | 最新記事、歩み削除、月間利用者数削除、プロフィール小型アイコン、メニュー外タップを維持しています。 |

## 反映方法

添付ZIPを展開し、フォルダ構成を変えずにGitHubリポジトリの `SancStudy` 直下へ上書きしてください。`assets/`、Supabaseのテーブル、RLS、SQLファイルは今回変更していません。

> `beeeeach` にも画像を表示したい場合は、Supabase Dashboard の `members` テーブルで該当メンバーの `avatar_url` に、HTTPSで公開できる画像URLを登録してください。
