# Supabase レビュー管理ガイド

SancStudyのレビュー機能は、Supabaseを使用して「管理者承認制」で運用されています。

## 1. レビューの承認手順
投稿されたレビューは、そのままではサイトに表示されません。以下の手順で承認を行ってください。

1. [Supabase Dashboard](https://supabase.com/dashboard) にログインします。
2. 対象のプロジェクトを選択します。
3. 左側メニューの **Table Editor** (テーブルアイコン) をクリックします。
4. `reviews` テーブルを選択します。
5. 新着のレビュー行を探し、**`approved` 列を `TRUE` に変更**します。
   - セルをダブルクリックしてチェックを入れ、保存してください。
6. これでサイト上にレビューが表示され、平均評価や統計にも自動的に反映されます。

## 2. セキュリティ（RLS）について
以下のポリシーが設定されており、安全に運用されています。
- **閲覧**: `approved = true` のデータのみ、誰でも閲覧可能です。
- **投稿**: `approved = false` 固定で、誰でも新規投稿可能です。
- **更新・削除**: サイト側（匿名ユーザー）からは一切できません。Supabaseの管理画面からのみ操作可能です。

## 3. データの削除
不適切なレビューが投稿された場合は、Table Editorから該当する行を削除してください。

## 4. 注意事項
- **Project URL** と **Publishable key (anon key)** は公開されても安全な設定（RLS）になっています。
- **Secret key (service_role)** やデータベースのパスワードは、絶対にサイトのコードに含めないでください。
